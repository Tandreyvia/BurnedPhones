using UnityEngine;
using System.Collections;

public class FixGoatSprite : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.localPosition = new Vector3(-0.02f, 0.22f, 0f);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
