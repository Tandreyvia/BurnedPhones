using UnityEngine;
using System.Collections;

public class FixCandleSprite : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.localPosition = new Vector3(-0.02f, 0.12f, 0f);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
